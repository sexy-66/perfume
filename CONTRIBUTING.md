# 贡献指南

感谢你愿意改进香方簿。提交前请先搜索现有 Issue，避免重复工作；较大的改动可以先开 Issue 讨论范围和用户影响。

## 开发流程

1. Fork 仓库并从 `main` 创建主题分支。
2. 在 `app` 目录执行 `flutter pub get`。
3. 修改代码和测试，保持改动聚焦。
4. 在提交前运行：

   ```bash
   dart format --output=none --set-exit-if-changed .
   flutter analyze
   flutter test -j 1
   ```

5. 提交 Pull Request，说明改动内容、用户影响和验证方式。

## 提交要求

- 新行为必须有对应测试，修复问题应包含可复现的回归测试。
- 不要提交 `build/`、`.dart_tool/`、`.qa/`、截图、APK、数据库快照、真实业务数据、签名密钥或密码。
- 不要把顾客资料、设备身份、配对凭据和局域网地址写入测试或日志。
- 业务计算保持整数定点规则，避免使用浮点数产生跨设备差异。
- 保持 Android 10+ 兼容性和现有中文界面体验。

## Pull Request 建议

- 一个 PR 只解决一个主题。
- 标题使用简短动词，例如 `修复香方图片裁切`。
- 描述中列出测试命令；涉及 UI 时附上必要的界面说明，但不要上传真实数据截图。
