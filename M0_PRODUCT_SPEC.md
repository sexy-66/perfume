# 香方簿 M0 产品与架构基线

- 文档状态：已冻结并验收；1.2 需求变更已确认
- 版本：1.2
- 日期：2026-08-08
- 冻结日期：2026-08-05
- 首版平台：Android
- 本文作用：后续产品、界面、数据结构、开发和验收的唯一需求基线

变更记录：

- 2026-08-05：取消资产购买链接；顾客资料仅支持编辑和删除，不设置停用状态；
- 2026-08-08：取消 SKU 模式，香料改为单层资料并直接持有图片；“合香珠”和“香牌”合并为一个制作类型；香料库增加长按多选删除和停用；所有弹窗入口必须防止连续点击重复打开；本次结构调整不保留旧版业务数据兼容，重建数据库后同步身份和私有配对凭据同时重置，设备需重新配对。
- 2026-08-08：恢复香料库右下角“添加香料”按钮；资产“添加第一项”点击即锁定，弹层关闭前不得重复查询或延长弹层；根页面无上一级时首次左滑或系统返回显示轻量“再按一次退出香方簿”提示，短时间内再次返回才退出应用。

## 1. 产品定位

香方簿是供店内店员使用的本地应用，用于：

- 建立香料、香牌和店内资产资料；
- 制作篆香、线香和“合香珠 / 香牌”时建立香方；
- 根据香方比例和目标总克重生成每种香粉的计划克重；
- 在实际调配过程中记录每种香粉的实际投放克重；
- 根据最终实际克重生成可复用香方；
- 保存顾客使用过的香方和全部调配记录；
- 在没有互联网和服务器时独立使用；
- 在店内多台 Android 设备之间通过局域网对等同步；
- 完整导出、导入、备份和恢复数据。

主要使用者是店内店员，顾客可以在店员操作的手机上参与香牌、合香珠或超限处理选择。

## 2. 首版范围

### 2.1 包含

- Android APK 直接安装和升级；
- 手机、平板、竖屏和横屏自适应布局；
- 完全本地离线运行；
- 空库交付；
- 用户自建制作类型、香料分类、资产分类和资产状态；
- 单层香料资料；
- 大类和香料推荐比例区间；
- 用户自建推荐配置；
- 新建香方、选择已有香方和顾客上次使用香方；
- 计划克重计算、实际投放记录和最终比例重算；
- 正式香方版本及来源追溯；
- 顾客快速建档和顾客使用历史；
- 香牌目录与调配时选择；
- 资产清点；
- 设备对等同步、手动同步、冲突保留和保护性重新加入；
- 最近删除；
- 自动滚动备份、手动完整导出和完整恢复；
- 用户图片压缩、去重和备份引用。

### 2.2 不包含

- iOS 和 Windows 版本；
- 云服务器、云账号或互联网同步；
- 应用关闭后的后台同步；
- AI 自动配香或内置推荐算法；
- 内置真实香料、分类、香牌、图片、比例、推荐配置或示例数据；
- 价格、订单、收款和销售管理；
- 香料库存、自动扣减、批次和保质期；
- 资产出入库流水和清点历史；
- 体质诊断、疗效或医疗建议；
- 员工账号、角色和权限系统；
- 应用内密码、指纹解锁或数据库密码；
- Excel、CSV、PDF、打印和配方分享；
- 深色模式；
- 多页首次使用教程；
- 一键将整个设备组回退到历史备份；
- 恢复出厂状态。

## 3. 空库交付与内容原则

首次安装后所有业务表为空。程序只提供结构、录入、计算、查询、同步和备份能力。

不得内置：

- 示例香料、顾客、香方或资产；
- 固定的“沾粉、主香、副香”等香料分类；
- 示例推荐区间或推荐配置；
- 香料、香牌或资产图片；

应用默认提供篆香、线香和“合香珠 / 香牌”三种制作类型，但用户可以新增、排序、停用和删除其他制作类型。“合香珠 / 香牌”在推荐配置、香方、筛选、历史和同步中始终作为同一个制作类型，不得拆成两个选项。

空资料页面只显示简洁说明和“添加第一项”按钮，不放示例数据，不使用 Emoji，不显示多页引导。

## 4. 资料库

### 4.1 香料分类

- 分类由用户自行创建；
- 每种香料只能属于一个固定分类；
- 分类采用平级结构；
- 分类包含名称、排序和停用状态；
- 分类可以停用或删除；
- 删除仍被当前香料使用的分类前，必须先完成香料重新分类或删除；
- 历史香方和调配记录始终显示当时的分类快照。

### 4.2 香料

香料采用单层管理，不建立或显示 SKU、供应商、产地和 SKU 推荐覆盖。

香料字段：

- 名称，必填；
- 一张主图，选填；
- 别名，选填；
- 固定分类，必填；
- 按制作类型设置的推荐比例区间，选填；
- 备注，选填；
- 停用状态。

点击添加香料后直接进入单层香料表单，完成名称、图片及其余香料字段录入；保存后即可用于推荐配置和香方。

### 4.3 推荐比例区间

- 大类和具体香料都可以设置推荐比例区间；
- 推荐区间按制作类型分别设置；
- 每个区间包含最低比例和最高比例；
- 比例允许两位小数；
- 最低值不得大于最高值；
- 未设置区间时仍可正常使用，只是不进行该项超限提醒；
- 区间只用于提示，不自动修改调配内容；
- 修改区间不影响历史香方或调配记录。

### 4.4 推荐配置

推荐配置由用户自行创建，不是 AI 推荐。

每个推荐配置包含：

- 名称；
- 一个适用制作类型；
- 大类及大类固定比例；
- 大类下具体香料及固定比例；
- 备注；
- 排序；
- 停用状态。

规则：

- 所有大类比例合计必须为 100%；
- 香料比例表示占整个香方的绝对比例；
- 同一大类下所有香料比例合计必须等于该大类比例；
- 可以只建立大类比例，但正式调配前必须补齐具体香料；
- 选择配置后复制到当前草稿，允许继续修改；
- 修改配置只影响以后使用，不影响已有草稿、香方或调配记录；
- 支持复制推荐配置后再修改。

### 4.5 香牌目录

香牌目录由用户自行录入，不设分类。

每个香牌包含：

- 名称，必填；
- 一张图片，选填；
- 规格或说明，选填；
- 备注，选填；
- 排序；
- 停用状态。

香牌在本次调配时选择并保存到调配记录，同一香方可以搭配不同香牌。

### 4.6 资产清点

资产包括但不限于压香器、插香板和模具。

资产分类和资产状态均由用户自行创建，包含名称、排序和停用状态。

每项资产包含：

- 名称，必填；
- 用户自建分类，必填；
- 一张图片，选填；
- 当前数量，必填且只能是非负整数；
- 存放位置，选填；
- 用户自建状态，选填；
- 备注，选填；
- 上次清点时间；
- 停用状态。

清点只覆盖当前数量并更新时间，不保留每次清点历史。

## 5. 顾客、香方与调配关系

### 5.1 顾客

顾客字段：

- 姓名，选填；
- 电话，选填；
- 备注，选填；
- 创建和修改时间。

姓名和电话不能同时为空。电话不强制为中国大陆手机号格式。

顾客资料仅支持编辑和删除，不设置停用状态。

新建香方不以顾客搜索为起点。顾客选择位于香方内部，可以选择已有顾客，也可以直接输入姓名或电话建立顾客并自动选中。

### 5.2 香方不归属顾客

- 香方本身不挂靠顾客；
- 顾客通过调配记录与香方产生关系；
- 通用香方可以不关联顾客；
- 某顾客使用通用香方后，本次调配显示在该顾客档案；
- 香方本身不显示挂靠了哪些顾客；
- 将一条顾客调配记录复制给另一顾客时，原顾客记录保留，新顾客获得一份副本。

顾客详情按香方合并展示：

- 香方名称；
- 最后使用日期；
- 使用次数；
- 点开后查看该香方的全部调配记录。

### 5.3 香方命名

- 用户可以主动填写香方名称；
- 有顾客姓名且名称留空时，使用顾客姓名第一个字加中国本地日期，例如“王260511”；
- 只有电话时，使用电话后四位加日期，例如“1380-260511”；
- 同日重名自动追加“-2”“-3”；
- 不增加独立姓氏字段，复姓同样取姓名第一个字；
- 没有关联顾客时允许创建通用香方，但必须手动填写香方名称；
- 没有顾客且名称为空时阻止完成，并要求补全名称。

### 5.4 新建香方

新建香方过程：

1. 选择制作类型；
2. 选择空白新建或推荐配置；
3. 选择具体香料并填写计划比例；
4. 选择性关联或直接创建顾客；
5. 输入目标总克重并进入调配；
6. 完成调配后，用最终实际比例创建正式香方初始版本。

调配完成前只是自动保存的草稿，不是正式香方。新建、调配和普通详情页面均不显示“V1”等版本字样。

### 5.5 选择已有香方

- 首页提供“选择已有香方”；
- 支持按名称和制作类型搜索；
- 默认使用最新正式版本；
- 旧版本从历史页面选择；
- 选择后输入本次目标总克重并进入调配；
- 普通重复调配不修改原香方版本；
- 本次最终比例保存在调配记录中；
- 顾客下次选择“上次使用过的香方”时，以该次调配的最终实际比例生成新计划，不使用原推荐比例。

### 5.6 香方版本

- 修改香方的香料、分类组成或固定比例时，进入“基于此香方调整”；
- 调整完成后创建新正式版本，并记录直接来源版本；
- 只修改目标总克重不创建版本；
- 修改香方名称或备注不创建版本；
- 顾客只关联调配记录，更换或复制顾客不影响香方版本；
- 正式版本只读；
- 从旧版本调整时，新版本指向实际使用的旧版本；
- 普通页面不显示版本字样；
- 只有历史页面显示 V1、V2、V3 等版本编号。

### 5.7 调配记录修改

- 调配完成后允许直接修改；
- 当前界面显示修改后的结果；
- 后台保留修改前数据、修改时间和修改设备；
- 修改历史在所有设备间同步并包含在完整备份中；
- 顾客下次使用修改后的最终比例；
- 修改前数据不再作为下次计划依据；
- 未完成调配自动保存，可以继续、取消或确认删除。

## 6. 比例与调配计算

### 6.1 输入与精度

- 创建香方时输入百分比；
- 比例允许两位小数；
- 全部香料比例合计必须为 100%；
- 调配时只使用克；
- 重量固定精度为 0.01g；
- 输入 2.1 后统一显示为 2.10g；
- 0.01g 是实际投放量，不是可忽略误差。

### 6.2 初始计划克重

```text
香料计划克重 = 目标总克重 × 香料计划比例 ÷ 100
```

按 0.01g 计算后产生的差额，自动加入当前计划克重最大的未填写香料；若并列则按列表顺序选择第一项。界面不额外标记这次分配。

### 6.3 实际投放

- 每个香料显示计划克重和实际克重输入框；
- 用户填入实际克重后，该值固定；
- 填写某项实际克重时，不修改其他香料的计划克重或实际克重；
- 不再强制保持最初目标总克重；
- 不提供“同步增加其他香粉”；
- 未填写项继续保留初始计划克重；
- 当前预计总重等于已填实际克重加未填计划克重；
- 当前预计比例随预计总重变化而重新计算。

示例仅说明算法，不属于内置推荐数据：

```text
最初目标 10.00g
计划：A 2.00g，B 6.00g，C 2.00g

用户把 A 实际填为 2.10g
B 仍为 6.00g
C 仍为 2.00g
预计最终总重变为 10.10g

预计比例：
A 20.79%
B 59.41%
C 19.80%
```

### 6.4 推荐区间提醒

- 每次填入实际克重后，使用“已填实际值＋未填计划值”估算最终比例；
- 同时检查大类和具体香料推荐区间；
- 第一次发现某项超限时，显示实际值、推荐区间和差值；
- 提供“继续使用”和“返回修改”；
- 用户已经确认过的同一超限在完成时不重复提示；
- 后续输入产生新的超限时，在完成前汇总提示一次；
- 用户确认后允许完成，不自动修改任何克重；
- 未设置推荐区间的项目不检查。

### 6.5 完成调配

- 用户点击“调配完成”后进入最终计算；
- 未填写实际克重的香料自动采用计划克重；
- 自动采用的值标记为“系统补全”，手工值标记为“手工填写”；
- 最终总克重等于所有最终克重之和；
- 最终香料比例等于该香料最终克重除以最终总克重；
- 最终大类比例由同类香料最终比例合计得到；
- 新建香方使用最终实际比例创建正式初始版本，不使用最初推荐比例；
- 普通重复调配只生成调配记录，不自动创建新版本；
- 基于正式香方进行主动调整时，最终实际比例创建来源可追溯的新版本；
- 调配记录保存选填自由文本备注。

## 7. 页面与交互

### 7.1 设计原则

- 简约、现代、安静、易读；
- 参考 Apple 和小米的信息层级与克制交互，但不照搬系统界面；
- 白色、浅灰、深色文字和一种克制蓝色；
- 优先保证文字、按钮和状态的可读性；
- 不刻意加入东方、香道、书法或传统纹样；
- 不使用蓝紫渐变、发光、玻璃拟态、聊天助手或明显 AI 风格；
- 不用 Emoji 代替功能图标；
- 使用统一系统线性图标；
- 正式 Logo 或品牌素材缺失时向用户索要，不自行生成正式素材；
- 不做深色模式；
- 触控区域不小于 48 × 48dp；
- 支持系统字体缩放和基本无障碍可读性。

以下内容用于说明希望接近的设计方向，不是可量化的自动验收门槛，也不得由代理据此反复打回实现：

- 以 Android 原生交互和克制的 Material 3 组件为基础；
- 不做通用 AI 仪表盘式的大标题欢迎区、渐变背景和悬浮光球；
- 不使用星光、魔法、机器人、聊天气泡等暗示 AI 的装饰或文案；
- 不把每一段内容都放进圆角卡片，只有真正需要分组时才使用卡片；
- 不把普通文字、筛选和状态全部做成胶囊按钮；
- 每个页面只有一个明确主操作，次要操作使用文字或弱化按钮；
- 标题、正文、辅助文字保持不超过三层清晰层级；
- 不使用无业务意义的大面积留白、抽象插画、吉祥物或装饰动画；
- 未收到用户素材前，不自行生成正式品牌图、空状态插画或产品图片；
- 推荐区间和自动计算使用普通业务措辞，不使用“智能推荐”“AI生成”等包装；
- 如果删除某项装饰不会降低信息清晰度，则删除该装饰；
- 表单和列表保持适合店员连续操作的信息密度，不为展示感牺牲效率。

视觉确认采用收敛规则：

- M4先提供新建香方、调配、香料库三个关键页面原型；
- M4开始设计前重新检索当时可用的成熟设计插件和Skill，不在M0提前锁死工具版本；
- 工具选择优先级为官方已安装能力、官方推荐插件、维护活跃且使用量较高的社区技能；
- 安装或使用前必须完整检查说明、适用范围、维护状态和许可，不因名称匹配就直接引入；
- 同一阶段最多选择一个主要设计Skill和一个原型或视觉验收工具，避免工具之间给出互相冲突的风格指令；
- 用户对原型作出最终视觉判断，不要求用户量化“AI味”或“过度设计”；
- 用户确认后的原型作为实现依据；
- 除非用户提出新要求，或实现出现可读性、触控、溢出等客观问题，不得仅凭主观审美重新开启视觉修改循环；
- 视觉工具和设计技能只辅助产生候选稿，不拥有最终否决权。

全局交互原则：

- 能静默完成的静默完成；
- 有安全默认值的直接采用；
- 能一步完成的不拆成多步；
- 正常保存、自动保存和同步成功不弹窗；
- 保存完成只短暂显示“已保存”；
- 只有删除、覆盖、首次超限、同步冲突等真实风险才要求确认；
- 删除、清空草稿和取消调配使用底部确认框。
- 任何会打开对话框、底部弹窗或选择器的入口，在当前弹层关闭或前置异步准备完成前只接受一次点击；连续点击不得排队打开多个相同弹层；
- 香料库普通模式保留右下角“添加香料”按钮，且添加入口不得与卡片“更多”操作区域重叠或互相遮挡，所有触控区域不小于 48 × 48dp；多选模式隐藏新增按钮。
- 应用根页面没有上一级界面时，左滑或系统返回首次只显示居中的轻量“再按一次退出香方簿”提示；短时间内再次返回才退出应用，不显示大确认对话框。
- “添加第一项资产”在点击开始时立即进入忙碌状态，前置查询和弹层关闭完成前忽略后续点击，不得延长弹层显示时间。
- “同步与设备”是已完成并验收的 M5 功能入口，正常状态显示“局域网设备与配对状态”；运行时初始化期间显示加载状态，失败时提供重试，不得显示“后续里程碑”或把入口永久禁用。

### 7.2 底部导航

```text
主页｜香方｜更多
```

“更多”页面顺序：

1. 香料库；
2. 香牌目录；
3. 顾客；
4. 推荐配置；
5. 资产清点；
6. 最近删除；
7. 同步与设备；
8. 备份与恢复；
9. 设置。

### 7.3 首页

首页显示：

- 新建香方；
- 选择已有香方；
- 最近使用香方；
- “合香珠 / 香牌”入口；
- 最近同步状态；
- 立即同步。

“合香珠 / 香牌”入口使用同一个制作类型进入香方选择或新建流程；需要搭配香牌时，再在调配流程中选择具体香牌，不再先把制作类型拆成香牌或合香珠。

### 7.4 列表、搜索与筛选

搜索范围：

- 香方：名称、制作类型；
- 香料：名称、别名、分类；
- 顾客：姓名、电话；
- 香牌：名称；
- 资产：名称、分类、位置、状态。

默认排序：

- 香方：最近使用优先；
- 香料：分类后按名称；
- 顾客：最近使用优先；
- 香牌：用户自定义顺序；
- 资产：分类后按名称；
- 推荐配置：用户自定义顺序。

筛选：

- 香方：制作类型；
- 香料：分类、停用状态；
- 香牌：停用状态；
- 资产：分类、状态、停用状态；
- 推荐配置：制作类型、停用状态。

香方列表每行显示：

- 香方名称；
- 制作类型；
- 最终比例最高的一到两种香料名称。

香料库列表交互：

- 普通点击进入香料详情或编辑；
- 长按任意香料进入多选模式，随后可点击其他香料增减选择；
- 多选模式提供“停用”和“删除”两个批量操作，并显示已选数量；
- 批量停用直接更新选中香料；批量删除使用一次破坏性确认；
- 退出多选模式后恢复普通添加、分类管理和单项操作。

香方列表不显示最近调配日期和最终总克重。

### 7.5 草稿与空页面

- 草稿和未完成调配持续自动保存；
- 返回时不弹保存确认；
- 返回后短暂显示“已保存草稿”；
- 不做首次使用教程；
- 空页面保留一个明确的“添加第一项”按钮。

### 7.6 图片

- 香料、香牌和资产各允许一张图片；
- 支持拍照或从相册选择；
- 统一方形裁剪；
- 静默压缩为最长边 1600 像素、JPEG 质量 85%；
- 不要求用户每次选择压缩方式。

### 7.7 自适应布局

- 基线为 Android 10 及以上；
- 支持手机和平板；
- 支持竖屏和横屏；
- 使用同一套自适应布局；
- 不开发平板专属业务功能；
- 开发验收前使用店内实际设备型号复核最低版本和布局。

## 8. 对等局域网同步

### 8.1 基本模型

- 店内预计使用 2～3 台 Android 设备；
- 没有固定主设备；
- 每台设备保存完整业务数据和图片；
- 每台设备均可完全离线新增、修改和调配；
- 已配对设备通过普通 Wi-Fi 或手机热点对等同步；
- 数据可以通过另一台设备间接传播；
- 同步不依赖互联网或外部服务器。

### 8.2 配对

- 任意已加入设备组的手机可以生成一次性六位配对码；
- 新设备输入配对码后复制完整业务数据和设备名单；
- 新设备获得自己的独立设备身份，不继承其他设备身份；
- 已配对设备自动发现彼此；
- 自动发现失败时允许手动输入对端地址；
- 设置页显示设备名称、最后同步时间和移除入口。

### 8.3 同步触发

- 应用启动时；
- 应用从后台回到前台时；
- 每次保存业务数据后；
- 首页点击“立即同步”；
- 设置页点击“立即同步”。

自动和手动触发共用同一同步逻辑。成功时静默更新状态，不弹成功提示。

只有应用打开或处于前台时才能被发现和响应同步，不要求应用关闭后运行。

发现多台在线设备时依次全部同步。同步中防止重复启动。

### 8.4 同步内容

同步：

- 制作类型、分类和香料；
- 推荐区间和推荐配置；
- 香方、版本、草稿、调配记录和修改历史；
- 顾客、香牌、资产、资产分类和状态；
- 图片；
- 停用、删除和恢复操作；
- 同步冲突及其解决结果。

不同步：

- 当前设备身份和私有配对凭据；
- 临时网络地址；
- 临时缓存；
- 仅属于当前设备的运行状态。

### 8.5 冲突

- 不按手机本地时间静默覆盖；
- 两台离线设备修改同一资料时保留双方版本；
- 同步本身继续完成；
- 首页显示“有待处理冲突”，不阻塞正常使用；
- 用户有空时选择保留一方或按需合并；
- 冲突、选择结果和修改历史同步到所有设备并进入完整备份。

### 8.6 暂时离线、正式移除与重新加入

- 长期未联网设备不会自动移除；
- 暂时离线设备仍可持续本地使用；
- 正式移除只用于手机丢失、报废或确定不再使用；
- 正式移除前显示强提醒；
- 被移除设备以后禁止同步，重新加入必须输入新配对码；
- 重新加入时先把未同步内容上传到待合并区；
- 新建且无冲突的数据可以合并；
- 两边修改过的数据保留双方；
- 设备组已删除而旧手机仍有的资料显示为“旧设备带回的资料”；
- 用户处理完成后，旧手机才接收设备组最新数据并恢复正常同步；
- 任何一方都不能被整库静默覆盖。

## 9. 停用、删除与最近删除

- 除顾客外，资料同时支持停用和删除；顾客仅支持编辑和删除；
- 停用资料不用于新内容，但历史正常显示；
- 删除资料进入最近删除，保留 30 天；
- 删除操作同步到所有设备；
- 任意设备可以在 30 天内恢复，恢复操作同样同步；
- 删除香料、香牌、香方或其他资料时，历史记录保留名称、比例、重量等文字快照；
- 历史记录不永久保留已删除资料的原图片；
- 删除顾客时让用户选择：仅删除顾客资料并保留不关联顾客的调配记录，或连同该顾客调配记录一起删除；
- 删除香方后，历史调配记录保留快照；
- 30 天后，且所有当前设备均确认收到删除、没有相关冲突时，清理完整资料和图片；
- 暂时离线设备会阻止相关删除完成清理；
- 正式移除设备后不再等待该设备确认；
- 数据库释放空间后优先重复利用；
- 后台保留最小同步水位和设备确认信息，不永久保留整条删除资料。

## 10. 备份、图片去重与恢复

### 10.1 自动备份

- 每天第一次打开应用时自动创建一次数据库快照；
- 保留最近 7 份；
- 应用升级数据库前额外创建升级前备份；
- 自动备份保存在应用内部目录；
- 卸载应用会删除内部自动备份，因此仍需手动外部导出用于换机。

### 10.2 图片去重

- 图片压缩后根据内容生成唯一标识；
- 相同图片在应用内部只保存一份；
- 7 份自动备份保存数据库快照和图片引用，不复制 7 份图片；
- 替换或删除的旧图片只保留到最后引用它的备份、最近删除或历史用途失效；
- 没有任何引用后自动清理；
- 实际空间接近“当前图片＋最近 7 天真正更换或删除的图片＋7 份小型数据库快照”。

### 10.3 手动完整导出

完整导出包含：

- 全部业务数据；
- 顾客资料；
- 香方、调配记录和修改历史；
- 同步冲突；
- 停用和删除状态；
- 用户上传的全部图片；
- 设备组公共信息；
- 数据格式版本、文件清单和完整性校验。

不导出当前设备私有身份和配对凭据。

完整备份不加密码，也不显示顾客资料提醒。每个人工导出的文件必须能独立恢复，因此每个外部导出文件都包含自身所需图片。

### 10.4 导入与恢复

- 导入前先校验格式和完整性；
- 导入前自动备份当前数据；
- 导入采用完整替换，不做备份文件与当前数据自动合并；
- 失败时保持或恢复原数据；
- 新手机导入后获得新设备身份，需要重新配对加入设备组；
- 导入用于换机、单机恢复或复制一份数据；
- 不提供一键把整个设备组回退到历史时间点；
- 首版不提供 Excel、CSV、PDF 或打印导出。

## 11. Android 权限与交付

- 只使用简体中文；
- 日期使用中国本地时间和 YYMMDD 格式；
- 提供签名的可直接安装 APK；
- 用户手动安装升级 APK；
- APK 升级保留全部业务数据和图片；
- 升级数据库前自动备份；
- 使用相机权限拍照；
- 使用系统图片和文件选择器导入图片、备份和导出文件；
- 使用局域网能力发现和同步设备；
- 不申请通讯录、短信、麦克风、后台通知或与业务无关的权限；
- 使用普通本地 SQLite，依赖 Android 系统锁屏保护本机数据。

## 12. 数据模型基线

所有业务主键使用设备离线可生成的唯一 ID。可变记录保存修改版本、来源设备、删除状态和同步信息。

| 数据表 | 主要内容 |
|---|---|
| `production_types` | 默认及用户新增制作类型、排序、停用状态 |
| `ingredient_categories` | 香料分类、排序、停用状态 |
| `ingredients` | 香料名称、图片、别名、固定分类、备注、停用状态 |
| `category_ratio_ranges` | 大类按制作类型的最低和最高比例 |
| `ingredient_ratio_ranges` | 香料按制作类型的最低和最高比例 |
| `recommendation_presets` | 推荐配置名称、制作类型、排序、备注、停用状态 |
| `recommendation_groups` | 推荐配置中的大类和固定比例 |
| `recommendation_items` | 推荐配置中的具体香料和绝对固定比例 |
| `customers` | 姓名、电话、备注 |
| `formulas` | 香方名称、备注、当前版本和最近使用信息，不关联顾客 |
| `formula_drafts` | 新建或调整中的自动保存草稿 |
| `formula_versions` | 版本号、直接来源版本、制作类型和创建时间 |
| `formula_items` | 版本内香料引用、分类和香料名称快照及固定比例 |
| `mixing_sessions` | 使用的香方或草稿、顾客、香牌、目标和最终总重、设备、备注、状态 |
| `mixing_items` | 计划克重、最终克重、手工或系统补全来源、最终比例 |
| `mixing_revisions` | 完成后修改前数据、修改时间和修改设备 |
| `plaque_types` | 香牌名称、图片、规格、备注、排序、停用状态 |
| `asset_categories` | 资产分类、排序、停用状态 |
| `asset_statuses` | 用户自建资产状态、排序、停用状态 |
| `assets` | 资产名称、分类、图片、整数数量、位置、状态、备注、清点时间 |
| `device_groups` | 店铺设备组公共身份 |
| `devices` | 设备名称、配对状态、最后同步时间和正式移除状态 |
| `sync_operations` | 可跨设备间接传播的新增、修改、删除和恢复操作 |
| `sync_cursors` | 各设备已确认的同步位置 |
| `sync_conflicts` | 双方冲突数据和处理结果 |
| `trash_entries` | 30天最近删除和恢复状态 |
| `media_objects` | 按内容标识去重的压缩图片和引用状态 |
| `backup_manifests` | 自动备份日期、数据库快照和媒体引用 |
| `app_settings` | 当前设备局部设置和界面设置 |

正式香方、调配记录和历史记录均保存必要文字快照，当前资料改名、停用或删除不得改变历史内容。

1.2 数据结构调整直接以新模型重建业务库，不迁移或兼容 1.1 及更早版本中的 SKU、香方、推荐配置和相关业务数据；设备组内所有设备必须升级到同一版本后重新录入业务资料。

## 13. 技术架构

- Flutter / Dart；
- Drift + SQLite；
- 图片保存在应用文件目录，数据库保存内容标识和相对路径；
- 每台设备是一份完整本地副本；
- 局域网同步使用对等操作日志和设备确认位置；
- 不建设云端服务；
- 首版真实数据规模按香料、顾客、香方和资产各几十条验收；
- 使用 Flutter 原生导航、简单状态管理和数据库数据流；
- 不为首版引入单实现接口、服务端框架或通用规则引擎。

## 14. 实施里程碑

| 里程碑 | 范围 | 验收结果 |
|---|---|---|
| M0 | 本文档、流程、计算、数据和同步边界 | 用户最终确认需求基线 |
| M1 | Flutter Android 工程、本地数据库、媒体目录、基础导航 | Android 10+ 空库安装和数据读写 |
| M2 | 制作类型、分类、单层香料、区间、推荐配置、香牌、顾客和资产 | 全部基础资料可增改查删；顾客以外资料支持停用 |
| M3 | 新建/选择香方、调配、最终比例、顾客历史、版本和修改历史 | 真实调配样例完整通过 |
| M4 | 页面视觉、自适应手机/平板及横竖屏、搜索筛选和可用性 | 真实设备完成日常操作 |
| M5 | 2～3台设备对等同步、配对、冲突、删除和保护性重新加入 | 已完成；模拟验收通过，真机验收由用户确认通过 |
| M6 | 自动备份、媒体去重、完整导出、导入和APK升级 | 数据和图片无损恢复 |
| M7 | 全量回归、性能、安装包和店内试用 | 正式 APK 交付 |

每个里程碑单独开发、检查和验收；上一个里程碑未通过前不进入下一个。

## 15. 最终验收清单

### 15.1 资料和界面

- [ ] 安装后没有任何业务示例数据；
- [ ] 用户可以创建制作类型、香料分类、资产分类和资产状态；
- [ ] 香料为单层资料，可直接录入名称和图片，不显示任何 SKU 入口或字段；
- [ ] 香料库长按进入多选，可批量停用和删除；
- [ ] 香料添加按钮与卡片更多操作不重叠；
- [ ] “合香珠 / 香牌”在推荐配置、香方和筛选中始终为一个制作类型；
- [ ] 连续点击任意弹窗入口只打开一个弹层；
- [ ] 推荐配置可复制，修改不影响历史；
- [ ] 首页、底部导航和更多页顺序符合本文；
- [ ] 香方列表显示比例最高的一到两种香料名称，不显示日期或克重；
- [ ] 普通页面不显示版本字样，历史页显示 V1、V2；
- [ ] 功能图标不使用Emoji，缺少正式品牌素材时向用户索取；
- [ ] 新建香方、调配、香料库三个关键页面原型已经用户视觉确认并固定为实现依据；
- [ ] 实现与已确认原型一致，不因代理对“AI味”的主观判断反复打回；
- [ ] 手机、平板、横屏和竖屏均可用；
- [ ] 图片方形裁剪并静默压缩。

### 15.2 顾客和香方

- [ ] 顾客姓名或电话至少填写一项；
- [ ] 顾客可以在香方内部直接创建；
- [ ] 香方本身不挂靠顾客；
- [ ] 顾客详情显示使用过的香方、最后日期、次数和调配记录；
- [ ] 通用香方无顾客时必须手动填写名称；
- [ ] 自动名称按姓名首字或电话后四位生成；
- [ ] 新建香方完成调配后才生成正式初始版本；
- [ ] 顾客下次使用上次最终实际比例；
- [ ] 主动调整香方时新版本能追溯直接来源；
- [ ] 已完成调配可直接修改并保留后台修改历史。

### 15.3 计算

- [ ] 比例两位小数并合计100%；
- [ ] 重量固定为克和0.01g精度；
- [ ] 计划克重差额自动加入最大未填写项且不额外标记；
- [ ] 手工实际克重不改变其他香料克重；
- [ ] 最终总重允许偏离目标总重；
- [ ] 未填项完成时按计划值系统补全；
- [ ] 最终比例从最终实际克重重新计算；
- [ ] 大类和香料区间按预计最终比例检查；
- [ ] 已确认超限不重复打扰，新超限只汇总提示一次；
- [ ] 新香方正式比例使用最终实际比例，不使用初始推荐比例。

### 15.4 同步、删除和备份

- [ ] 单台设备完全断网可用；
- [ ] 2～3台设备在Wi-Fi和热点下对等同步；
- [ ] 启动、回前台、保存和手动操作都能触发同步；
- [ ] 数据可通过中间设备间接传播；
- [ ] 冲突保留双方且不阻塞正常使用；
- [ ] 长期离线设备可持续录入；
- [ ] 正式移除设备重新加入时保护性合并；
- [ ] 最近删除30天内可跨设备恢复；
- [ ] 已删除资料不会被旧设备静默复活；
- [ ] 删除内容在全设备确认后可清理，不无限保留完整图片；
- [ ] 每日自动备份保留7份且图片不重复7份；
- [ ] 手动导出可以在新手机完整恢复数据和图片；
- [ ] 新手机恢复后使用新设备身份并重新配对；
- [ ] APK升级前自动备份，升级后数据不丢失。

## 16. 当前状态

M0 已由用户于 2026-08-05 整体确认并冻结。本文件是后续开发的需求基线。

- 当前不初始化 Flutter 工程或 Git 仓库；
- M1 只在新的独立开发任务中开始；
- 后续发现的新需求不得静默改写 M0，应先记录变更内容、影响范围和所属里程碑，再由用户明确确认；
- 纯文字澄清且不改变产品行为时，可以直接补充说明。
